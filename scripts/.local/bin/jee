#! /usr/bin/env dash
SFILE=$(fd -H --type f -E .git -E .gitignore -E .github -E *.png -E *.jpg -E *.jpeg -E *.svg  . | fzf --height=50% --reverse) && emacsclient -nc $SFILE

  ## This is made using emacs
